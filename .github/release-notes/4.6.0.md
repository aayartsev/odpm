## 4.6.0

### English

**4.6.0** is the recommended version for everyday use. It replaces **4.5.0**. Pre-release **4.6.0-beta** checked that `.deb` / `.rpm` packages install, APT/YUM repos work, and `pip install` succeeds; stable is available from APT/YUM suite **`stable`**, regular PyPI (`pip install odpm`), and [stable documentation](https://aayartsev.github.io/odpm/stable/en/install/).

#### What's new in this release

- **Targeted docker-compose changes** — in an extended `odpm.json` you can patch the built-in Odoo and PostgreSQL services (ports, environment, command) without replacing the whole service block. Extra containers can set `command` and `entrypoint`. See [odpm.json](https://aayartsev.github.io/odpm/stable/en/reference/odpm-json/).

- **Plugin compose patches** — plugins may supply small compose adjustments; settings from your project's `odpm.json` take priority. Plugins written for the previous API still load. See [Plugins](https://aayartsev.github.io/odpm/stable/en/reference/plugins.md).

- **Compose checks before start** — odpm validates the generated `docker-compose.yml` structure during prepare and reports problems early (for `developer`, `server`, and `ci` scenarios).

- **Variables in compose and hooks** — `${VAR}` in extra services and in shell hook commands; optional `user` and `tty` for auxiliary containers. See [odpm.json](https://aayartsev.github.io/odpm/stable/en/reference/odpm-json/) and [plugins](https://aayartsev.github.io/odpm/stable/en/reference/plugins.md).

- **Odoo settings in git** — store team settings such as `proxy_mode`, `workers`, or `dbfilter` in `odpm.json`; they apply inside the container when odpm builds `odoo.conf`, without overwriting your local file on disk. Keys managed by odpm itself cannot be overridden. See [odoo.conf](https://aayartsev.github.io/odpm/stable/en/reference/odoo-conf.md).

- **Clearer `odpm plan`** — fewer false warnings when you use `--plan-strict` on projects that already have a materialized runtime config.

#### Versions (short)

| Question | Answer |
|----------|--------|
| Which odpm version is installed? | **4.6.0** (`odpm --version`) |
| Must I change `odpm.json` in my project? | **No** — existing projects keep working; the extended nested format is optional |

#### Install

Verify checksums using **`SHA256SUMS`** from this GitHub Release.

| Platform | How to install |
|----------|----------------|
| Debian/Ubuntu | download `odpm_4.6.0-1_all.deb` or add the APT repo (**stable** suite) — [stable guide](https://aayartsev.github.io/odpm/stable/en/install/linux-deb/) |
| Fedora 40+ | download `odpm-4.6.0.fc*.noarch.rpm` or add the YUM repo (**stable**) — [stable guide](https://aayartsev.github.io/odpm/stable/en/install/fedora-rpm/) |
| pip | `pip install odpm` |

```bash
# Debian/Ubuntu — from downloaded file
sudo apt install ./odpm_4.6.0-1_all.deb

# Fedora — from downloaded file (match your fc* version)
sudo dnf install ./odpm-4.6.0.fc*.noarch.rpm

# pip
pip install 'odpm==4.6.0'

odpm --version
# expected: odpm version: 4.6.0
```

#### Documentation

- [Installing odpm (stable)](https://aayartsev.github.io/odpm/stable/en/install/) — recommended for production
- [Local dev from scratch (stable)](https://aayartsev.github.io/odpm/stable/en/getting-started/local-dev-from-scratch/) — demo project [odoo_demo_project](https://github.com/aayartsev/odoo_demo_project) (Odoo 19.0)
- [Plugins (stable)](https://aayartsev.github.io/odpm/stable/en/reference/plugins.md)
- Archived beta: [4.6.0-beta install guide](https://aayartsev.github.io/odpm/4.6.0-beta/en/install/)
- Full changelog for developers: [CHANGELOG.md](https://github.com/aayartsev/odpm/blob/v4.6.0/CHANGELOG.md)

#### Feedback

If install or upgrade failed — [open an issue](https://github.com/aayartsev/odpm/issues): say how you installed (deb / rpm / pip), paste `odpm --version` output, and what went wrong.

---

### Русский

**4.6.0** — рекомендуемая версия для повседневной работы. Заменяет **4.5.0**. Предварительная **4.6.0-beta** помогла проверить установку из `.deb` и `.rpm`, а так же работу APT/YUM-репозиториев и работу команды `pip install`; стабильный выпуск доступен в ветке APT/YUM **`stable`**, через обычный `pip install odpm` и в [документации stable](https://aayartsev.github.io/odpm/stable/install/).

#### Что нового в этой версии

- **Точечные правки docker-compose** — в расширенном `odpm.json` можно менять встроенные сервисы Odoo и PostgreSQL (порты, переменные окружения, команда запуска), не переписывая весь блок сервиса. У дополнительных контейнеров можно задать `command` и `entrypoint`. См. [odpm.json](https://aayartsev.github.io/odpm/stable/reference/odpm-json/).

- **Правки compose от плагинов** — плагины могут предлагать небольшие изменения compose; настройки из `odpm.json` вашего проекта важнее. Плагины под предыдущий API по-прежнему загружаются. См. [Плагины](https://aayartsev.github.io/odpm/stable/reference/plugins.md).

- **Проверка compose до запуска** — при подготовке окружения odpm проверяет структуру сгенерированного `docker-compose.yml` и сообщает о проблемах заранее (сценарии `developer`, `server`, `ci`).

- **Переменные в compose и хуках** — `${VAR}` в дополнительных сервисах и в командах shell-хуков; при необходимости можно указать параметры `user` и `tty` у вспомогательных контейнеров. См. [odpm.json](https://aayartsev.github.io/odpm/stable/reference/odpm-json/) и [плагины](https://aayartsev.github.io/odpm/stable/reference/plugins.md).

- **Настройки Odoo в git** — параметры вроде `proxy_mode`, `workers` или `dbfilter` из `odoo.conf` можно хранить в `odpm.json`; они попадут в финальный файл `odoo.conf` внутри контейнера, не перезаписывая ваш локальный файл на диске. Ключи, которыми управляет сам odpm, переопределить нельзя. См. [odoo.conf](https://aayartsev.github.io/odpm/stable/reference/odoo-conf.md).

- **`odpm plan` стал вести себя спокойнее** — меньше ложных предупреждений при `--plan-strict`, если runtime-конфиг проекта уже собран.

#### Версии (кратко)

| Вопрос | Ответ |
|--------|-------|
| Какая версия odpm установлена? | **4.6.0** (`odpm --version`) |
| Нужно ли менять `odpm.json`? | **Нет** — существующие проекты работают как раньше; переход на расширенный вложенный формат осуществляется по желанию |

#### Установка

Сверяйте checksum по **`SHA256SUMS`** из GitHub Release.

| Платформа | Способ |
|-----------|--------|
| Debian/Ubuntu | `odpm_4.6.0-1_all.deb` или APT **stable** — [stable](https://aayartsev.github.io/odpm/stable/install/linux-deb/) |
| Fedora 40+ | `odpm-4.6.0.fc*.noarch.rpm` или YUM **stable** — [stable](https://aayartsev.github.io/odpm/stable/install/fedora-rpm/) |
| pip | `pip install odpm` |

```bash
sudo apt install ./odpm_4.6.0-1_all.deb
sudo dnf install ./odpm-4.6.0.fc*.noarch.rpm
pip install 'odpm==4.6.0'
odpm --version
# ожидается: odpm version: 4.6.0
```

#### Документация

- [Установка odpm (stable)](https://aayartsev.github.io/odpm/stable/install/)
- [Локальная разработка с нуля (stable)](https://aayartsev.github.io/odpm/stable/getting-started/local-dev-from-scratch/)
- [Плагины (stable)](https://aayartsev.github.io/odpm/stable/reference/plugins.md)
- Архив beta: [4.6.0-beta](https://aayartsev.github.io/odpm/4.6.0-beta/install/)
- [CHANGELOG.md](https://github.com/aayartsev/odpm/blob/v4.6.0/CHANGELOG.md)

#### Обратная связь

Если установка или обновление не удались — [issue](https://github.com/aayartsev/odpm/issues): способ установки (deb / rpm / pip), вывод `odpm --version`, описание ошибки.
