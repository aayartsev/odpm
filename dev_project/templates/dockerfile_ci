# #MESSAGE#
FROM {BASE_IMAGE}

WORKDIR {DOCKER_PROJECT_DIR}

COPY --chown={CURRENT_USER}:{CURRENT_USER} . .

USER {CURRENT_USER}

RUN python3 -m bake.bake_venv --config {CI_BAKE_DIR}/{CI_VENV_INSTALL_JSON}
